; Disable strict peer dependency enforcement due to payloadcms issues
strict-peer-dependencies=false

# Allow native build scripts for required packages
allowBuilds.esbuild=true
allowBuilds.sharp=true
allowBuilds.unrs-resolver=true
